# Files for systemd
These files should be copied to `/etc/systemd/system/` and enabled using `systemctl`.

Make sure to replace `RGBMATRIX_WORKING_DIR` and `RGBMATRIX_VENV_DIR` with the appropriate paths or export them as environment variables (e.g. adding to `.bash_rc`).
